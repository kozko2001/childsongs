
const initialState = {
  animalSelected: null,
  animals: ['tortuga', 'marieta', 'escarbat'],
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}